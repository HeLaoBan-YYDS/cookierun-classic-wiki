# Cloudflare Pages 静态重定向规则
# 静态导出模式下没有 middleware / server 端重定向，
# 所有跳转都通过 _redirects 在边缘完成。
# 注意：每条规则独占一行，格式为 `<from>  <to>  <status>`。

# 防御：用户外链 / 历史 /en 入口时 301 到无前缀英文版
# （默认 locale 的英文页由 app/page.tsx 渲染到根，不在 /en/ 下）
/en                  /                  301
/en/*                /:splat            301

# next 静态导出在 root 不会自动生成 404.html，
# Cloudflare Pages 会自动兜底渲染 _redirects 的最后一条 fallback，
# 但我们提供一个真实的 404.html（见 public/404.html）来保证体验一致。

